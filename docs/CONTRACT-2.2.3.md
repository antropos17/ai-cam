# Block 2.2.3 — Play Mode snapshot capture: полный ратифицированный контракт

> **РАТИФИЦИРОВАН человеком 2026-08-04.** Черновик написан 2026-08-04 по
> заказу «contract first, no code»; прецеденты структуры и строгости —
> `docs/CONTRACT-2.2.1.md` и `docs/CONTRACT-2.2.2.md`. Адъюдикация
> 2026-08-04: решения №1–№8, №10 — первой передачей; №11 (гейт-драйвер
> среды) — добавлен по находке сессии и закрыт; развилка строки 11 —
> отдельным решением по данным П-1 (state fidelity, не спецслучай); №9
> (литералы) — РАТИФИЦИРОВАН с четырьмя поправками: литерал 6 получает
> хвост «граница охвата, не дефект»; литерал 10 разделён на 10a/10b (запрет
> встроенного условия в user-facing строке); литерал 11 заменён по развилке;
> строка капсулы дополнена веткой сна (требование решения №4). Остальные
> литералы ратифицированы дословно как переданы отчётом. Решения финальны
> для блока, если человек явно не пере-откроет. Эмпирические предпосылки
> П-1…П-5 выполнены 2026-08-04, все зелёные — измеренные значения записаны
> в разделе «Эмпирические предпосылки». Любое противоречие между этим
> файлом, PLAN и STATUS = STOP и адъюдикация, не угадывать.
>
> **ПОПРАВКИ 2026-08-05 — пять; провенанс разделён явно.** Тела поправок 1,
> 2 и 5 и уточнение строк 9/12 получены от человека целыми. Три зачина
> (поправка 3, поправка 4, первая строка блока RULES) реконструированы
> агентом и ПОДТВЕРЖДЕНЫ человеком 2026-08-05. Весь прочий вспомогательный
> текст раздела — цитаты кода, координаты строк, обоснования и сноски —
> написан агентом и поимённо атрибутирован в «Провенансе», подраздел
> «Авторство вспомогательного текста (2026-08-05)»; ратификация человеком на
> него не распространяется, кроме отдельно названных решений 2026-08-05:
> союз AND в поправке 3 и привязка `SNAPSHOT_CAPTURE_FAILED` к структурной
> точке входа вместе со сноской в «Коде ошибки». Предмет поправок, раздел
> «Поправки 2026-08-05» ниже: (1) `SceneCaptureHandover.cs` в поверхности изменений и
> три новых поля `BodyDto`; (2) ключи статики наружу не выставляются
> (сопоставление внутри `SceneCapture.Capture`, решение Р1(a)); (3)
> структурная точка входа запуска — непубличная и с другим именем; (4)
> сравниваемые множества тел и статик — раздельные, `ExcludedSafely` вне их;
> (5) строка 12 и канал масштаба, разрешённые замером
> `Library/BugCamEvidence/probe-2.2.3-v1-pins/REPORT.md`. Прежний текст
> разделов не переписывается и остаётся видимым — действует редакция
> поправки (конвенция «Поправка 2026-08-04» `CONTRACT-2.2.2.md`). Код блока
> по-прежнему не начат.

## Провенанс

Инструкции заказа черновика пришли фрагментами с обрывами при передаче
(прецедент — memory `user-messages-truncated` и раздел «Провенанс»
`CONTRACT-2.2.1.md`). Реконструированы по смыслу и подлежат подтверждению
человеком: начало пункта «what is captur|ed: which fields of the stride-14
record…», заголовок списка «…for |my adjudication, numbered…», начало
ограничения «the draft is a draft |exactly as 2.2.2 was written…», начало
STOP-условия «if a cl|aim depends on an API you have not verified…», начало
STOP-условия «if you cannot find a r|esolution that keeps the existing pins
bit-identical…». Если между обрывками были потеряны целые пункты — они в этом
черновике отсутствуют и должны быть добавлены при адъюдикации.

Подтверждение 2026-08-04: человек сверил все пять реконструкций с исходным
текстом — совпали, потерянных пунктов нет. Сообщение адъюдикации 2026-08-04
тоже пришло с двумя обрывами, восстановленными однозначно по контексту:
«…exact A|PI names» (правило: непроверенные API помечать с точными именами,
всё непроверенное — в разделе «Непроверенные утверждения») и «Tow|er or
domino pin drift: stop» (STOP-условие пинов башни/домино).

Сообщение 2026-08-04 «Ruling 9 and the literal-11 fork» дошло частично
(секция развилки — целиком, тело решения №9 оборвано); тело решения №9
получено повторной передачей в той же сессии, с одним обрывом,
восстановленным однозначно: «…|: ratified verbatim, unchanged» = «Literals
1–5, 7–9, 12: ratified verbatim, unchanged» (согласуется с «everything else
accepted verbatim as you pasted it» в первой части того же решения).
Языковой прецедент сверен до правок: ратифицированные литералы 2.2.1/2.2.2
и новые литералы этого контракта — на одном языке (русский с латинскими
API-терминами), STOP-условие перевода не сработало.

Сообщение поправок 2026-08-05 пришло тем же образом — фрагментами с
обрывами (тот же прецедент). Тела поправок 1, 2, 5 и уточнение по строкам 9
и 12 получены целиком. Реконструированы по смыслу ровно три зачина —
**сверены человеком с исходным текстом и ПОДТВЕРЖДЕНЫ 2026-08-05**
(решение R1 того же дня): начало поправки 3 «…|point: a failure before
Play Mode entry writes no evidence capsule…» (записано как «структурная
точка входа запуска»), начало поправки 4 «…|d set comparison: the snapshot
uses the same membership criterion…» (записано как «сравниваемые
множества»), и начало блока RULES «…|uses that are not changing» (прочитано
как «не переписывать клаузы, которые не меняются»). Остальные правила блока
RULES получены целиком: датировать каждую поправку, оставлять
превзойдённый текст видимым, STOP на коллизии с ратифицированным текстом
2.2.1/2.2.2/2.2.3, STOP с цитированием обеих сторон при расхождении кода с
формулировкой поправки, PLAN/STATUS — только по требованию
согласованности, ветка `feat/block-2.2.3-impl` от `main`, PR без мержа.
Если между обрывками были потеряны целые пункты — их в этой редакции нет и
они должны быть добавлены отдельной адъюдикацией.

### Авторство вспомогательного текста (2026-08-05)

Список ниже перенесён ДОСЛОВНО из read-only-ревью агента 2026-08-05
(отсюда первое и второе лицо в формулировках) и не пере-выводился, не
объединялся, не разбивался, не перенумеровывался и не дополнялся. Это
места, где текст написан агентом, а не получен от человека; сам текст
остаётся на месте и становится атрибутированным, а не удалённым.
Координаты строк — исторические, на коммит `03e417a`, и НЕ пересчитаны под
текущую редакцию. Отдельно подтверждено человеком 2026-08-05: три зачина
(R1), союз AND в поправке 3 (R2), привязка `SNAPSHOT_CAPTURE_FAILED` к
структурной точке входа вместе со сноской в «Коде ошибки» (R3).

1. `03e417a: docs/CONTRACT-2.2.3.md lines 621–628` (поправка 1) — состав
   нынешнего `BodyDto`, ссылка `SceneCaptureHandover.cs:38-50`, ссылка на
   шаг 6 потока и перечисление «нулевая угловая скорость, бодрствующими,
   дефолтный предел». В сообщении этого нет.
2. `03e417a: docs/CONTRACT-2.2.3.md lines 630–633` (поправка 1) — абзац
   `Fail-closed здесь не срабатывает…`.
3. `03e417a: docs/CONTRACT-2.2.3.md lines 640–643` (поправка 2) — цитаты
   `SceneCapture.cs:249-253`, `:522`, `:599`, `:149`.
4. `03e417a: docs/CONTRACT-2.2.3.md lines 647–649` (поправка 2) —
   `Ратифицированного текста контракта отзыв не касается…`.
5. `03e417a: docs/CONTRACT-2.2.3.md lines 655, 661–664, 667–669`
   (поправка 3) — идентификатор кода, четыре конкретных пути/диапазона
   строк, `(все четыре сверены в этом репозитории при внесении поправки)`,
   `(`GhostEvidencePlayModeHost.cs:78-81`)`.
6. `03e417a: docs/CONTRACT-2.2.3.md lines 678–680` (поправка 3) — весь абзац
   про инвариант П.6.
7. `03e417a: docs/CONTRACT-2.2.3.md line 686` (поправка 4) —
   `(`SceneCapture.cs:24`; бесконтактные тела и т.п.)`.
8. `03e417a: docs/CONTRACT-2.2.3.md lines 695–697` (поправка 4) — `Общий
   счётчик или общее множество ключей…`.
9. `03e417a: docs/CONTRACT-2.2.3.md lines 710–714` (поправка 5) — цитата
   док-комментария, `SimulationHarness.cs:234-235`, `SceneCapture.cs:835`.
10. `03e417a: docs/CONTRACT-2.2.3.md lines 610–613` (преамбула раздела) —
    `Коллизий с ратифицированным текстом 2.2.1, 2.2.2 и 2.2.3 при внесении
    не найдено…`: это моя оценка, не ваше слово.
11. `03e417a: docs/CONTRACT-2.2.3.md`, раздел «Непроверенные утверждения»,
    пункт `**Открытый пункт реализации (добавлен 2026-08-05 поправкой 5):**`
    — формулировка приписывает пункт поправке, тогда как поправка о входном
    канале молчит; пункт мой.
12. Вне контракта, тем же коммитом: `03e417a: docs/STATUS.md` — абзац, и
    `03e417a: docs/PLAN.md` — вставка; оба пересказывают поправки моими
    словами.

## Интент (из `docs/PLAN.md` Block 2.2.3, внесён 2026-08-04)

Источником начального состояния поиска становится **снимок живой Play
Mode-сессии** вместо захвата edit-mode-сцены. Пользователь запускает игру,
доигрывает до интересного момента, жмёт «снимок» — BugCam берёт текущее
физическое состояние (позиция, вращение, линейная скорость, угловая скорость,
sleeping — ровно поля существующего stride-14 рекордера,
`StateRecorder.WriteBody`, `Assets/BugCam/Core/StateRecorder.cs:78-104`) как
начальное состояние поиска дивергенции в быстрой локальной PhysicsScene.

## Жёстко вне охвата (заявляется в контракте явно)

- **Gameplay-скрипты не воспроизводятся.** Их результат становится начальным
  состоянием — и только им. Это прямое продолжение границы `SPEC.md` §13 и
  раздела «Жёсткое ограничение v0.1» контракта 2.2.2: захватывается физическая
  сцена, не игровая логика.
- **Полный Play Mode-реплей с повторным исполнением скриптов** — backlog
  с пометкой «after first sale» (дословно из PLAN Block 2.2.3 OUT).
- Никаких новых физических фич, никакого DOTS, никакой 2D-физики.
- Существующие гарантии детерминизма, пины и fail-closed-поведение —
  без изменений (раздел «Пины»).

## Общие правила блока

- Ветка `feat/block-2.2.3`; коммиты `block-2.2.3: <what>`; evidence в
  `Library/BugCamEvidence/Block2.2.3-*`.
- STOP на находках; никаких молчаливых изменений семантики; сдвиг любого пина
  из раздела «Пины» = STOP и адъюдикация.
- Ядро правил A2 и контракта 2.2.2 не переписывается — блок аддитивен.
- Код — только после ратификации этого контракта и исполнения эмпирических
  предпосылок (раздел «Эмпирические предпосылки до кода»).

## Центральный вопрос: инвариант П.6 контракта 2.2.2 — предлагаемое разрешение

Инвариант П.6 (`CONTRACT-2.2.2.md`, Поправка 2026-08-04, п. 6): «ни один путь
запуска не входит в симуляцию, минуя захватную edit-mode-точку» — полный
`contentHash` вычислим только в edit mode ДО входа в Play Mode (эмпирика
2026-08-04: геометрия меша с выключенным Read/Write в Play Mode нечитаема).
Живой снимок по определению происходит ПОСЛЕ входа в Play Mode. Конфликт
разрешается так (Адъюдикация 2026-08-04, решение №1 — ПРИНЯТО как
рекомендовано: П.6 сохраняется дословно, без поправок):

**Снимок не является захватом.** Снимок записывает ТОЛЬКО состояние
(stride-14 на тело + позы статики + сверочные скаляры) и НИКОГДА — геометрию,
меш-ссылки или структуру сцены. Авторитетный захват (геометрия, meshRef,
полный contentHash, структурный отпечаток) выполняется там же, где сегодня:
в edit mode, внутри `GhostEvidencePlayModeHost.StartTowerSearch`, ДО
переключения `EditorApplication.isPlaying`
(`Assets/BugCam/Editor/GhostEvidencePlayModeHost.cs:101-134`), после того как
пользователь вышел из игровой сессии. Снимок инъецируется в результат этого
захвата как замена начального состояния. Отсюда точные ответы:

1. **Где происходит меш-резолв на снапшот-пути:** в захватной
   edit-mode-точке запуска поиска — бит-в-бит тот же путь, что у scene-рана
   2.2.2 (`SceneCapture.Capture` → `SceneCaptureHandover.Store`,
   `GhostEvidencePlayModeHost.cs:112-122`). Снапшот-путь не добавляет ни
   одной новой точки меш-резолва.
2. **С чем сравнивается симуляционный структурный отпечаток:** с
   `SimulationMeshReference` (vertexCount, subMeshCount, bounds), записанным
   ЭТИМ edit-mode-захватом и переданным раннеру через `SceneCaptureHandover`
   (`Assets/BugCam/Editor/SceneCaptureHandover.cs`);
   `SimulationHarness.ResolveMeshForSimulation`
   (`Assets/BugCam/Core/SimulationHarness.cs:707-765`) не меняется ни в одной
   строке. Инвариант П.6 сохраняется дословно, без поправок.
3. **Тело живой сессии без пары в edit-mode-захвате** (runtime-spawned):
   fail-closed на точке запуска с именованной verbatim-причиной и кодом
   ошибки снапшот-пути (таблица отказов, строка 6; Адъюдикация 2026-08-04,
   решения №2 и №5 — оба приняты; условия решения №2 — в разделе
   «Сопоставление и инъекция»).

**Отвергнутая альтернатива** (Адъюдикация 2026-08-04, решение №1: запись
ратифицирована именно затем, чтобы вопрос не пере-открывался): поиск
изнутри той же Play Mode-сессии без выхода в edit mode. Отвергается, потому
что (а) полный contentHash в Play Mode невычислим — пришлось бы ослаблять
капсулу до структурной сверки, ровно того, что П.6 запрещает, и что капсула
2.2.2 честно называет слепой зоной; (б) сегодняшний код уже закрывает этот
путь: запуск scene-рана при `EditorApplication.isPlaying == true` очищает
handover (`GhostEvidencePlayModeHost.cs:112-122`, ветка else →
`SceneCaptureHandover.Clear()`), и раннер падает fail-closed с
`SCENE_MESH_RESOLVE_FAILED` — это закреплённое поведение, а не случайность;
(в) хост гасит только ту Play Mode-сессию, которую сам начал
(`GhostEvidencePlayModeHost.cs:131-133`) — поиск внутри пользовательской
сессии сломал бы и этот ратифицированный контракт окна.

## Поток запуска (снапшот-путь, пошагово)

1. Пользователь входит в Play Mode сам (хост не участвует; ключ
   `EnteredPlayModeKey` не ставится — сессия пользовательская).
2. Пользователь жмёт «снимок» в окне. Снимок пишется в SessionState-handover
   (см. «Снимок»); окно показывает отчёт снимка. Игра продолжается — снимок
   её не останавливает.
3. Пользователь выходит из Play Mode сам.
4. Пользователь запускает поиск (окно/меню → `TryStartTowerSearch`).
   В edit mode, до `isPlaying = true`, выполняется авторитетный захват
   активной сцены — тот же вызов, что у scene-рана 2.2.2.
5. Сопоставление снимка с захватом (см. «Сопоставление и инъекция»);
   несовпадение = fail-closed, поиск не стартует, Play Mode не включается.
6. Успех: определения тел получают состояние из снимка; хэш захвата
   вычисляется по инъецированным определениям (Адъюдикация 2026-08-04,
   решение №6 — ратифицировано); handover несёт
   уже инъецированный результат; дальше — существующий путь 2.2.2 без
   изменений (Play Mode → раннер → `SimulationHarness.Run`).

## Снимок: поля, момент, атомарность, персистентность

- **Поля на динамическое тело — ровно stride-14 и ничего больше:**
  `rigidbody.position` (3), `rigidbody.rotation` (4), `rigidbody.linearVelocity`
  (3), `rigidbody.angularVelocity` (3), `rigidbody.IsSleeping()` (1) — тот же
  набор и порядок, что пишет `StateRecorder.WriteBody`
  (`StateRecorder.cs:88-103`) и читает `StateRecorder.WriteRigidbodies`
  (`StateRecorder.cs:106-134`). Чтение — через Rigidbody-API, НЕ через
  `transform`: при включённой интерполяции transform внутри кадра может
  отставать от состояния тела (стандартное поведение Unity; в этом репозитории
  эмпирически НЕ проверено — предпосылка П-4).
- **Дополнительно на тело:** `rigidbody.mass` и размер/масштаб по формуле
  существующего захвата — для строк дрейфа таблицы отказов (в симуляцию не
  идут); **`rigidbody.maxAngularVelocity` — поле ВОСПРОИЗВЕДЕНИЯ** (решение
  развилки строки 11, 2026-08-04): идёт в симуляцию наряду с состоянием,
  чтобы воспроизведённое тело жило с тем же пределом, что живое.
- **Позы статики и кинематики (Адъюдикация 2026-08-04, решение №3 — принято
  с поправкой):** снимок записывает мировые position/rotation И lossyScale
  всех статик-кандидатов (включая кинематические тела, замораживаемые в
  статику правилом A2) по их существующим ключам; позы (position/rotation)
  идут в симуляцию — «результат скриптов становится начальным состоянием»
  распространяется на кинематические платформы; принуждение статики назад к
  edit-mode-позам дало бы внутренне противоречивое начальное состояние.
  **Поправка решения №3:** расхождение position/rotation со значениями
  edit-mode-захвата допустимо и ФИКСИРУЕТСЯ в метриках рана (аддитивный
  список ключей разошедшихся статик с обеими позами); расхождение МАСШТАБА =
  fail-closed (строка 12 таблицы) — масштаб питает objectScale и меш-логику
  отказов 2.2.2 и из снимка не переносится. Допуск сравнения масштаба
  заявлен явно в «Сопоставлении и инъекции»: нулевой, бит-в-бит.
- **Момент снимка (Адъюдикация 2026-08-04, решение №10 — принято с
  требованием):** один синхронный проход по всем телам в одном
  main-thread-колбэке редактора, **без yield'ов** (требование ратифицировано).
  Физика игровой сцены шагает только в player loop на том же потоке, поэтому
  проход атомарен относительно физического шага: снимок семантически равен
  «состояние после последнего завершённого физического шага N, до шага N+1».
  Пауза редактора НЕ обязательна. В метаданные снимка ОБЯЗАТЕЛЬНО пишутся
  **индекс физического шага** (число завершённых фиксированных шагов игровой
  сцены на момент снимка) и **`Time.fixedTime`** (ратифицировано решением
  №10), плюс `Time.frameCount` (информационно; wall-clock не участвует).
  Риск бес-паузного снимка покрывается предпосылкой П-3. Конкретный способ
  получения индекса шага — на реализацию; кандидат-вычисление помечен в
  «Непроверенных утверждениях».
- **Персистентность:** JsonUtility-DTO в SessionState — тот же механизм и та
  же планка, что `SceneCaptureHandover` (бит-точность float закреплена пином
  `SceneCaptureHandoverRoundTripsBitIdentically`,
  `SceneCaptureHandover.cs:14`); снимок обязан пережить domain reload входа
  в Play Mode на шаге 6 потока. Для снапшот-handover — свой аналогичный
  бит-точность-пин (обязательный тест).

## Сопоставление и инъекция

Сопоставление снимка с edit-mode-захватом — по существующей схеме stable ID
(hierarchy path + sibling index; ключи статики — текущая схема `#NN`/`#rb`,
`SceneCapture.cs:597-602`). Правила:

- Множества динамик и статик обязаны совпасть **в обе стороны и точно**:
  тело/статика из снимка без пары в захвате И тело/статика из захвата без
  пары в снимке — fail-closed (строки 6, 7, 10a, 10b таблицы). Runtime-spawned
  объект, сдвинувший sibling-индексы существующих объектов, ломает ключи
  МНОГИХ записей — это тоже честный fail-closed по несовпадению множеств,
  катастрофический, но безопасный. **Адъюдикация 2026-08-04, решение №2 —
  принято с двумя условиями:** (а) сообщение отказа обязано ПОИМЁННО назвать
  тела-нарушители и заявить, что это граница охвата v0.1, а не дефект
  (выполнено: ратифицированный литерал 6 несёт хвост «это граница охвата
  v0.1, не дефект» — решение №9); (б) записана
  backlog-запись без кода: захват примитивных (Box/Sphere) runtime-спаунов
  по шейпу — кандидат на поздний блок (раздел «Backlog-записи блока»).
- `mass` и размер тела сверяются снимок↔захват; дрейф = fail-closed
  (строки 8, 9). **Допуск всех скалярных сверок снимок↔захват (масса,
  размер, масштаб) — НУЛЕВОЙ, сравнение бит-в-бит** (заявлено явно по
  требованию решения №3): обе стороны читают одно и то же неизменённое
  значение той же арифметикой (сериализованное значение либо та же
  TRS-композиция трансформа), поэтому любое расхождение вообще — реальное
  расхождение; прецедент — Поправка 2026-08-04 п. 2 контракта 2.2.2;
  смешанный допуск `HasShear` здесь явно неприменим. Остальные
  Rigidbody-параметры не сверяются: захват fail-closed'ит нестандартные
  значения СВОИХ проверяемых полей (damping, constraints, Discrete,
  авто-COM/инерция — правила A2/2.2.2 без изменений);
  `maxAngularVelocity` в их число НЕ входит (находка П-1) и потому
  переносится из снимка как поле воспроизведения, а не предполагается
  дефолтным (решение развилки строки 11).
- **Инъекция при успехе:** в `SimulationBodyDefinition` каждого тела
  подставляются из снимка `Position`, `Rotation`, `InitialLinearVelocity`
  плюс новые аддитивные поля `InitialAngularVelocity`, `StartsAsleep`
  (решение №4) и `MaxAngularVelocity` (решение развилки строки 11 —
  per-body-предел из снимка, применяется в `CreateBody` ДО присвоения
  угловой скорости); в `SimulationStaticColliderDefinition` — позы
  position/rotation из снимка (решение №3, ратифицировано с поправкой о
  масштабе). Геометрия, meshRef, contentHash, отпечаток, `Size`, `Mass`,
  `FullScale`, `Shape`, масштаб — только из захвата, из снимка НИКОГДА.
- Возмущение применяется к инъецированной позиции цели тем же кодом, что
  сегодня (`SimulationHarness.CreateBody`, `SimulationHarness.cs:796-801`);
  выбор цели — существующий dropdown по телам захвата (display-name
  provider, решение №3 контракта A1).

**Поправки 2026-08-05 к этому разделу (текст выше оставлен видимым):**
поправка 2 — сопоставление живёт внутри `SceneCapture.Capture`, ключи
`#NN`/`#rb` наружу не выставляются; поправка 4 — критерий членства тот же,
что у обычного захвата, минус меш-резолв, `ExcludedSafely` вне сравниваемых
множеств, тела и статики сравниваются РАЗДЕЛЬНО; поправка 5 — сравнение
масштаба (строка 12) идёт по внутреннему каналу
`StaticCandidate.SourceLossyScale` до конструирования `SceneCaptureResult`
и до сериализации handover.

## Новые каналы начального состояния (аддитивная поверхность Core)

Сквозной канал линейной скорости уже существует: захват читает
`rigidbody.linearVelocity` (`SceneCapture.cs:653`) → 
`SimulationBodyDefinition.InitialLinearVelocity` (`SimulationHarness.cs:178`)
→ `CreateBody` присваивает `rigidbody.linearVelocity`
(`SimulationHarness.cs:831`). Отсутствуют и добавляются аддитивно (новый
хвостовой конструктор, существующие сигнатуры verbatim — правило пинов A1/2.2.2):

1. **`InitialAngularVelocity` + `MaxAngularVelocity` (развилка строки 11
   ЗАКРЫТА человеком 2026-08-04 по данным П-1 — против fail-closed на
   дефолтных 50 rad/s):** поднятый `maxAngularVelocity` — нормальная
   практика в сценах машин и спиннеров, отказ на дефолте отверг бы
   легитимные проекты. Реализуется как ВЕРНОСТЬ СОСТОЯНИЮ, не спецслучай:
   снимок несёт per-body `rigidbody.maxAngularVelocity` (поле
   воспроизведения наряду с mass/size), определение тела получает аддитивное
   поле `MaxAngularVelocity`, и `CreateBody` применяет его ДО присвоения
   `rigidbody.angularVelocity` — воспроизведённое тело живёт с тем же
   пределом, что живое. Fail-closed ТОЛЬКО когда угловая скорость снимка
   превышает СОБСТВЕННЫЙ maxAngularVelocity этого тела из снимка (строка 11
   таблицы): такое состояние истинно невоспроизводимо — первый `Simulate`
   клампит его (замер П-1: 150 при max 50 → ровно 50 после первого шага), и
   симуляция расходится с живой сессией с шага 0. Обоснование аддитивности
   поля: A2 сегодня `maxAngularVelocity` не сверяет (находка П-1) — поле
   аддитивное, а не предполагаемое.
2. **`StartsAsleep` (Адъюдикация 2026-08-04, решение №4 — принято условно
   на П-2, как написано):** при `true` `CreateBody` вызывает
   `rigidbody.Sleep()` после присвоения состояния, ЕСЛИ предпосылка П-2
   подтверждает жизнеспособность; иначе — откат к прецеденту A2 «стартует
   бодрствующим». **Требование решения №4: какая бы ветка ни была взята,
   вердикт-вывод И капсула улик обязаны явно называть её; молчаливый откат
   к «стартует бодрствующим» запрещён.** Существующий адъюдицированный
   notice A2 «спит на момент захвата — стартует бодрствующим»
   (`SceneCapture.cs:634-643`) на НЕ-снапшот-путях сохраняется без изменений
   в любом исходе.

`StateRecorder`, `DivergenceEngine`, `EpsilonSearch` — без изменений: stride-14
уже несёт все поля снимка (утверждение PLAN подтверждено кодом,
`StateRecorder.cs:88-103`).

## Хэш захвата — аддитивность (Адъюдикация 2026-08-04, решение №6 — ратифицировано)

Существующие строки `D|`/`S|` уже несут позицию и линейную скорость
(`SceneCapture.cs:1015-1019`), поэтому инъекция снимка ДО `ComputeHash` даёт
хэш, честно описывающий фактическое начальное состояние симуляции. Правило
аддитивности (прецедент — хвост `|mesh:` 2.2.2):

- На НЕ-снапшот-путях все hash-строки **байт-в-байт прежние** — пины башни
  (`40e46640…` домино, `8b8e9a94…` GateScene222) не сдвигаются по построению.
- Только на снапшот-пути строка `D|` получает хвостовой сегмент с новым
  состоянием: `|state:<angVel "R" invariant>:<sleeping 0|1>:<maxAngularVelocity
  "R" invariant>` (третий сегмент добавлен решением развилки строки 11 —
  предел входит в воспроизводимое состояние, объявлено не молчаливо);
  строка `S|`
  при позах-из-снимка меняется только значениями существующих полей
  (формат строки не меняется).

**Требование решения №6 (обязательный тест):** несущее свойство — hash-строки
edit-mode-пути не меняются, а пины башни и домино остаются байт-идентичными —
закрепляется ПРЯМЫМ регрессионным тестом (не-снапшот-захват производит
байт-в-байт прежние hash-строки), а не косвенно через существующие пин-тесты.

## Manifest и капсула улик

- Секция `sceneCapture` — без изменений ключей (правило 2.2.2). Добавляется
  аддитивная секция снимка (имя секции — `snapshot`, ратифицировано в
  составе контракта 2026-08-04):
  `scenePath`, **индекс физического шага** и **`fixedTime`** (обязательны —
  решение №10), `frameCount`, счётчик тел, sha-256-хэш состояния снимка по
  stride-14-флоатам в `"R"` invariant (стиль существующих hash-строк), флаг
  на тело `sleeping` на момент снимка, **фактически взятая ветка сна**
  (перенос / стартует бодрствующим — требование решения №4) и **список
  ключей статик, чьи позы разошлись с edit-mode-захватом, с обеими позами**
  (поправка решения №3; тот же список входит в метрики рана).
  `SchemaVersion` (= 1) не меняется — поля аддитивные (прецедент — решение
  №8 контракта 2.2.2).
- **Честная строка капсулы (РАТИФИЦИРОВАНА 2026-08-04, решение №9, с
  дополнением ветки сна по решению №4):**
  «начальное состояние получено снимком живой Play Mode-сессии; gameplay-
  скрипты в симуляции не воспроизводятся — их результат стал начальным
  состоянием; захват геометрии и hash выполнены в edit mode при запуске
  поиска, а не в момент снимка; ветка сна: перенос сна через
  Rigidbody.Sleep()».
  Ветка названа фиксированным текстом, а не встроенным условием (прецедент —
  поправка литерала 10 в этом же решении): П-2 зелёная ⇒ ветка А решения №4
  выбрана окончательно; переход на ветку Б потребовал бы пере-открытия
  решения №4 и правки этой строки как отдельной адъюдикации.

## Таблица отказов (литералы РАТИФИЦИРОВАНЫ дословно 2026-08-04 — решение №9 с четырьмя поправками)

Точки: **снимок** (Play Mode, кнопка), **запуск** (edit mode, сверка до
`isPlaying = true`), **симуляция** (раннер/харнесс). Все строки таблицы
отказов 2.2.2 действуют без изменений; таблица ниже — только новые строки
снапшот-пути.

| # | Ситуация | Точка | Причина (verbatim, РАТИФИЦИРОВАНА дословно 2026-08-04) |
|---|---|---|---|
| 1 | кнопка снимка вне Play Mode | окно (disabled-причина) | «снимок доступен только в Play Mode — запустите игру и дойдите до нужного момента» |
| 2 | активная сцена не сохранена (нет scenePath) | снимок | «снимок в несохранённой сцене не поддерживается — сцену не с чем сопоставлять при запуске» |
| 3 | NaN/Infinity в состоянии тела | снимок | «снимок содержит нечисловое состояние (NaN/Infinity) тела «<path>»» |
| 4 | снапшот-ран без снимка (handover пуст/повреждён) | запуск/симуляция | «снимок отсутствует или повреждён — сделайте снимок в Play Mode заново» |
| 5 | сцена запуска ≠ сцена снимка | запуск | «снимок сделан в сцене «<путь из снимка>», а поиск запускается в сцене «<активный путь>»» |
| 6 | тело из снимка без пары в захвате (runtime-spawned) | запуск | «тело «<path>» существует в снимке, но отсутствует в edit-mode-сцене — runtime-объекты не поддерживаются в v0.1 — это граница охвата v0.1, не дефект» |
| 7 | тело из захвата без пары в снимке (уничтожено/переименовано) | запуск | «тело «<path>» есть в edit-mode-сцене, но отсутствует в снимке — объект уничтожен или переименован во время игры» |
| 8 | масса разошлась | запуск | «масса тела «<path>» изменилась между снимком и запуском (<из снимка> ≠ <из захвата>)» |
| 9 | размер разошёлся | запуск | «размер тела «<path>» изменился между снимком и запуском (<из снимка> ≠ <из захвата>)» |
| 10a | статика есть только в снимке | запуск | «набор статик не совпадает: «<path>» присутствует только в снимке» |
| 10b | статика есть только в захвате | запуск | «набор статик не совпадает: «<path>» присутствует только в захвате» |
| 11 | угловая скорость в снимке превышает СОБСТВЕННЫЙ `maxAngularVelocity` тела — состояние истинно невоспроизводимо (первый `Simulate` клампит, расхождение с живой сессией с шага 0) | снимок | «угловая скорость тела «<path>» (<значение> rad/s) превышает собственный maxAngularVelocity этого тела (<предел> rad/s) — состояние невоспроизводимо: первый шаг симуляции клампит её» |
| 12 | (добавлена поправкой решения №3) масштаб статики/кинематики разошёлся снимок↔захват | запуск | «масштаб статики «<path>» изменился между снимком и запуском (<из снимка> ≠ <из захвата>) — масштаб не переносится из снимка» |

**Статус литералов (решение №9, 2026-08-04): РАТИФИЦИРОВАНЫ дословно.**
Литералы 1–5, 7–9, 12 — verbatim как переданы отчётом; четыре поправки
человека внесены: литерал 6 — хвост «это граница охвата v0.1, не дефект»
(закрывает условие (а) решения №2); литерал 10 — разделён на 10a/10b:
встроенное условие внутри user-facing строки отвергнуто как дефект в
ожидании — verbatim-проверка такой строки невозможна; литерал 11 — заменён
по решению развилки (тело, значение, собственный предел тела); строка
капсулы — дополнена веткой сна (решение №4, без молчаливого умолчания).

**Поправки 2026-08-05 к таблице (литералы строк не меняются):** строки 9 и
12 печатают полный `Vector3` — три компоненты, формат `"R"`,
`InvariantCulture`; `objectScale` для них использовать нельзя (сворачивает
вектор в одну компоненту и пропустил бы анизотропное изменение масштаба).
Строки 6/7 и 10a/10b адресуются раздельными множествами тел и статик —
поправка 4.

Отказ на точке запуска не включает Play Mode вовсе (шаг 5 потока); отказ на
точке симуляции поднимается структурной ошибкой Core-результата и доносится
fail-closed-документом улик (правило CLAUDE.md о `readonly struct`-результатах,
прецедент 2.2.2).

## Код ошибки (Адъюдикация 2026-08-04, решение №5 — ратифицировано)

Новый run-level-код **`SNAPSHOT_CAPTURE_FAILED`** для точек «снимок» и
«запуск» снапшот-пути. `SCENE_CAPTURE_FAILED` остаётся за захватом,
`SCENE_MESH_RESOLVE_FAILED` — за симуляционным меш-резолвом
(`Assets/BugCam/Evidence/GhostEvidenceSchema.cs:110,119`) — оба без изменений.
Обоснование — то же, что у решения №5 контракта 2.2.2: для пользователя
«снимок не сопоставился со сценой» и «сцена не захватилась» — разные
инциденты с разными действиями по исправлению.

**Поправка 3 (2026-08-05) к доставке кода:** отказ до входа в Play Mode не
пишет капсулу улик и не создаёт каталог рана, поэтому код возвращается
структурно рядом с причиной отказа — через новую непубличную точку входа с
другим именем (см. «Поправки 2026-08-05», поправка 3).

## Факт среды проекта (для гейта; не часть продукта)

`ProjectSettings/DynamicsManager.asset:21` — `m_SimulationMode: 2` (Script;
подтверждено проходящими assert'ами `Physics.simulationMode ==
SimulationMode.Script` во всех PlayMode-тестах). Следствие: в живой Play
Mode-сессии ЭТОГО проекта физика игровой сцены сама не шагает — сцена
GateScene222 при входе в Play Mode неподвижна. Гейт-драйвер (временный,
некоммитимый скрипт — прецедент `Gate222Automation.cs`) обязан на время
сессии переключить `Physics.simulationMode` в FixedUpdate рантайм-присвоением
ИЛИ сам шагать `Physics.Simulate` — выбор за исполнением гейта. Требование:
сериализованный `DynamicsManager.asset` после гейта БЕЗ изменений
(рантайм-присвоение не пишет в ассет — эмпирически НЕ проверено,
предпосылка П-5). Для целевого пользователя ограничения нет: типичный проект
живёт в дефолтном FixedUpdate-режиме.

**Сопутствующая находка гейта (замер 2026-08-04):** расфокусированный
редактор в Play Mode НЕ качает player loop вовсе — `Time.frameCount` и
`fixedTime` замирают (наблюдался frame, застывший на 701 через три
последовательных обращения); `EditorPrefs InteractionMode=1` снимает
троттлинг редакторских апдейтов, но игровой цикл не запускает. Лечится
рантайм-присвоением `Application.runInBackground = true` — гейт-драйвер
обязан выставить его; при выходе из Play Mode значение автоматически
откатывается в False, сериализованные настройки не дирти́тся (замерено).

**Адъюдикация 2026-08-04, решение №11 (новое, по этой находке) — три
условия:** (а) драйвер — строго test-only и НИКОГДА не попадает в плагин;
(б) evidence гейта обязан фиксировать, что гейт шёл под временным драйвером —
результат гейта НЕЛЬЗЯ читать как доказательство работы снапшот-пути в
дефолтном проекте; (в) обязательный backlog-пункт (до любых заявлений об
общей поддержке): проверить снапшот-путь в проекте с дефолтным
`simulationMode = FixedUpdate` — см. «Backlog-записи блока».

## Пины, обязанные остаться бит-в-бит

1. **Башенный гейт (дефолты, factory-путь):** threshold `1.98919879E-05` м,
   вилка `1.978456E-05…1.98919879E-05`, ширина `1.07427695E-07`, кадр 27
   (body 49), spread `0.003794474` (body 5), 21/49, амплификация
   `190.753891`; field-diff metrics/manifest пуст (кроме
   runId/builtUtc/scenePath-метаданных).
2. **Домино-гейт A8 (captured-scene путь, дефолты):** threshold
   `0.0022972275`, вилка `0.00228482112…0.0022972275`, кадр 3 (body 1), 1/5,
   амплификация `1.00000107`; **hash захвата `40e46640…b5b6531` без
   изменений**.
3. **GateScene222 (edit-mode-захват в стиле 2.2.2):** hash
   `8b8e9a94…44b927` воспроизводится; meshRef на всех 10 объектах.
4. **Счётчики тестов:** EditMode 157 / PlayMode 27 растут только новыми
   тестами; ни один существующий тест не ослабляется и не удаляется.
5. Хэш-аддитивность: все hash-строки НЕ-снапшот-захватов байт-в-байт прежние;
   `objectScale` Box/Sphere-тел бит-в-бит (пин 2.2.2); score-половина
   AND-гейта — закрытая адъюдикация 2026-08-03, блок её не касается.
6. Инвариант П.6 и вся таблица отказов 2.2.2 — дословно без изменений.

## Эмпирические предпосылки до кода (обязательные; прецедент 2.2.2) — ВЫПОЛНЕНЫ 2026-08-04, все зелёные

**Измеренные значения (живой редактор Unity `6000.3.21f1`, mcp-for-unity
`execute_code`, локальные PhysicsScene в Play Mode; методика и полный вывод —
отчёт сессии 2026-08-04):**

- **П-1 (кламп):** присвоение `rigidbody.angularVelocity` НЕ клампится
  (assign 150 rad/s при max 50 → readback 150); кламп происходит на первом
  `Simulate` — ровно до `maxAngularVelocity` (150 → 50.0 после первого шага;
  дальше угловое демпфирование: 49.95); при поднятом max 200 значение 150
  переживает шаг (149.85 после демпфирования). Дефолтный
  `maxAngularVelocity` в этом проекте = **50 rad/s**. Исход: развилка
  строки 11 закрыта человеком 2026-08-04 (см. «Новые каналы», пункт 1).
- **П-2 (сон):** `Rigidbody.Sleep()` сразу после инстанциирования в свежей
  локальной PhysicsScene ДЕРЖИТСЯ: 10/10 шагов сон, y=0.5 без сдвига;
  `Sleep()` ОБНУЛЯЕТ предустановленные скорости (lin (1,2,3) и ang (1,1,1) →
  (0,0,0) — согласуется со снимочной стороной: спящее живое тело отдаёт
  нулевые скорости); пробуждение от удара упавшего тела на шаге 27, после
  успокоения тело снова заснуло (финальный y=0.500006855). **ЗЕЛЁНАЯ ⇒
  действует ветка А решения №4 — перенос сна.**
- **П-3 (бит-повтор):** два прогона с инъецированным состоянием (ненулевые
  линейные и угловые скорости, повёрнутое тело в воздухе, спящее тело;
  32 шага × 3 тела × 14 флоатов = 1344 значения) — **0 бит-расхождений**.
- **П-4 (Rigidbody-API против transform):** при
  `RigidbodyInterpolation.Interpolate` `transform.position` расходится с
  `rigidbody.position` между кадрами на **0.010–0.274 м** (4 сэмпла на
  падающем теле: 0.0100097656 / 0.0543823242 / 0.0279541016 / 0.274230957)
  — снимок обязан читать Rigidbody-API; два чтения `rb.position` в одном
  синхронном колбэке бит-идентичны (4/4); кандидат индекса шага
  `fixedTime/fixedDeltaTime` даёт почти-целые (97.000005, 103.000002,
  124.000002, 145.000002, 168.000001) — требуется округление.
- **П-5 (`simulationMode`):** рантайм-присвоение
  `Physics.simulationMode = FixedUpdate` НЕ дирти́т `DynamicsManager.asset`
  (git status пуст) и автоматически откатывается к Script уже ПРИ ВЫХОДЕ из
  Play Mode (не только в следующей сессии); свежая play-сессия стартует со
  Script; `enterPlayModeOptions=None` без дрейфа. Явный restore в драйвере
  остаётся требованием решения №11 (пояс и подтяжки поверх авто-отката).

Определения предпосылок (как поставлены до замера):

- **П-1. Кламп угловой скорости:** присвоение `rigidbody.angularVelocity`
  выше дефолтного `maxAngularVelocity` в свежей локальной PhysicsScene —
  переживает ли значение присвоение и первый `Simulate`; измерить фактический
  дефолтный предел. Исход и решение развилки — в статус-блоке выше
  (развилка строки 11 закрыта 2026-08-04).
- **П-2. Перенос сна:** `Rigidbody.Sleep()` сразу после инстанциирования в
  локальной PhysicsScene — остаётся ли тело спящим через N шагов `Simulate`
  до первого контакта; каковы `linearVelocity`/`angularVelocity` спящего тела
  при чтении (снимочная сторона). По исходу выбирается ветка уже закрытого
  условного решения №4 (перенос при зелёной П-2, иначе прецедент A2) —
  новой адъюдикации исход не требует, взятая ветка называется в вердикте и
  капсуле.
- **П-3. Бит-идентичный повтор снапшот-пути:** два поиска от одного
  сохранённого снимка → field-diff metrics/manifest пуст (кроме
  runId/builtUtc) — расширение пина A5 на новый путь.
- **П-4. Rigidbody-API против transform при интерполяции:** чтение
  `rigidbody.position/rotation` в editor-колбэке между кадрами при включённой
  интерполяции тела — совпадает ли с состоянием после последнего шага и
  расходится ли с `transform.position`.
- **П-5. Рантайм-присвоение `Physics.simulationMode`:** не дирти́т ли
  сериализованный `DynamicsManager.asset` и возвращается ли Script в
  следующей сессии (нужно только гейту, не продукту).

## Поверхность изменений (карта для ревью; код — после ратификации)

- **Core:** `SimulationBodyDefinition` — аддитивный хвостовой конструктор с
  `InitialAngularVelocity`, `StartsAsleep` и `MaxAngularVelocity` (развилка
  строки 11; существующие конструкторы verbatim,
  `SimulationHarness.cs:90-190`); `CreateBody` — применение
  `maxAngularVelocity` ДО присвоения угловой скорости, затем присвоение
  скоростей и опциональный `Sleep()` (`SimulationHarness.cs:827-832`);
  `SceneCapture.ComputeHash` — хвост `|state:` только для снапшот-определений.
  `ResolveMeshForSimulation`, `StateRecorder`, `DivergenceEngine`,
  `EpsilonSearch*` — НЕ трогаются.
- **Editor:** снимочный модуль (чтение stride-14 + поз статики по
  Rigidbody-API, один синхронный проход); снапшот-handover
  (SessionState/JsonUtility, прецедент `SceneCaptureHandover`); ветка
  снапшот-пути в `StartTowerSearch` (сопоставление + инъекция ДО
  `isPlaying = true`); `GhostSearchEntry` — новое значение
  `GhostSearchSceneKind.SnapshotScene = 2` (Адъюдикация 2026-08-04, решение
  №8 — ратифицировано; аддитивно, существующие значения не
  перенумеровываются; SessionState-персистентность entry уже пишет
  `SceneKind` как int, `GhostEvidencePlayModeHost.cs:161`); окно — кнопка/состояния снимка
  (ASCII-макет секции — отдельная ратификация человеком до реализации,
  прецедент поправки-макета A1).
- **Evidence:** писатель — секция снимка; схема — код `SNAPSHOT_CAPTURE_FAILED`
  (решение №5 — ратифицировано); честная строка капсулы.
- **Tests:** EditMode — пины всех строк таблицы отказов verbatim, обе стороны
  несовпадения множеств, дрейф массы/размера, дрейф масштаба статики
  (строка 12), хэш-аддитивность (НЕ-снапшот-строки байт-в-байт + формат
  хвоста `|state:`), **прямой регрессионный тест решения №6**
  (не-снапшот-захват производит байт-в-байт прежние hash-строки — отдельным
  тестом, не косвенно через пины), бит-точность снапшот-handover (аналог
  `SceneCaptureHandoverRoundTripsBitIdentically`), пин строки 11 (снимочный
  angVel против собственного per-body-предела); PlayMode — инъекция угловой
  скорости и сна в локальной PhysicsScene, воспроизведение тела с поднятым
  `maxAngularVelocity` (угловая скорость выше дефолтных 50 rad/s переживает
  первый шаг — по замеру П-1), бит-идентичный повтор снапшот-пути (П-3).

**Поправки 2026-08-05 к этой карте (список выше оставлен видимым,
дополняется):**

- **Editor, добавляется файл:** `Assets/BugCam/Editor/SceneCaptureHandover.cs`
  — `BodyDto` получает `angularVelocity`, `startsAsleep` и
  `maxAngularVelocity` (поправка 1; без них инъецированное состояние
  теряется на domain reload и снапшот-ран молча вырождается в обычный
  scene-ран).
- **Editor, форма точки входа:** новая структурная точка входа запуска —
  непубличная и с ДРУГИМ именем; публичный `TryStartTowerSearch` остаётся
  байт-в-байт и делегирует в неё (поправка 3; запрещённые формы перечислены
  там же).
- **Core, `SceneCapture`:** приватное поле `StaticCandidate.SourceLossyScale`
  и сравнение масштаба статик внутри `SceneCapture.Capture` до
  конструирования `SceneCaptureResult`; `SceneCaptureResult` и
  `SimulationStaticColliderDefinition.FullScale` не расширяются (поправка 5).
- **Tests:** пин строки 12 читает полный `Vector3` в `"R"`/`InvariantCulture`
  (уточнение строк 9 и 12); рефлексионные тесты
  `GhostEvidenceTests`/`SearchEntryParameterizationTests` не переписываются
  и не ослабляются (поправка 3); round-trip-пин handover расширяется на три
  новых поля `BodyDto` (поправка 1).

## Поправки 2026-08-05 (пять; провенанс текста разделён — см. ниже)

> Приняты 2026-08-05 — после того как завершился эмпирический замер,
> гейтивший поправку 5. **Провенанс текста этого раздела:** тела поправок 1,
> 2 и 5 и уточнение строк 9/12 получены от человека целыми; три зачина
> (поправки 3 и 4 и первая строка блока RULES) реконструированы агентом и
> подтверждены человеком 2026-08-05; остальной вспомогательный текст —
> цитаты кода, координаты строк, обоснования и сноски — написан агентом и
> атрибутирован в «Провенансе», подраздел «Авторство вспомогательного
> текста (2026-08-05)». Ратификация человеком не распространяется на
> агентский вспомогательный текст, кроме отдельно названных решений
> 2026-08-05 (союз AND в поправке 3; привязка `SNAPSHOT_CAPTURE_FAILED` к
> структурной точке входа вместе со сноской в «Коде ошибки»). Разделы выше
> не переписываются, их текст остаётся видимым; там, где поправка меняет
> редакцию, действует текст поправки (конвенция «Поправка 2026-08-04»
> `CONTRACT-2.2.2.md`). **Проверка агента при внесении (2026-08-05, не
> ратифицированный вывод):** коллизий с ратифицированным текстом 2.2.1,
> 2.2.2 и 2.2.3 агент не нашёл — по его оценке все пять поправок аддитивны
> или уточняют место, а не отменяют решение. Код блока не начат ни одной
> строкой.

### Поправка 1 (2026-08-05) — `SceneCaptureHandover.cs` в поверхности изменений

`Assets/BugCam/Editor/SceneCaptureHandover.cs` добавляется в раздел
«Поверхность изменений» как файл, который БУДЕТ изменён. Его `BodyDto`
обязан нести `angularVelocity`, `startsAsleep` и `maxAngularVelocity`.

Сегодня `BodyDto` несёт `stableId`, `position`, `rotation`, `size`, `mass`,
`velocity`, `shape`, `meshRef`, `fullScale` (`SceneCaptureHandover.cs:38-50`)
— канала для угловой скорости, сна и per-body-предела в нём нет. Поток
запуска (шаг 6) требует, чтобы handover нёс УЖЕ инъецированный результат:
без этих трёх полей инъецированное состояние теряется на domain reload при
входе в Play Mode, и снапшот-ран **молча вырождается в обычный
scene-ран** — тела стартуют с нулевой угловой скоростью, бодрствующими и с
дефолтным пределом, ран отрабатывает до конца и выдаёт вердикт.

**Причина обязательности записывается именно так: вырождение молчаливое, а
не отказ.** Fail-closed здесь не срабатывает — ран выглядит успешным, а
начальное состояние уже другое; поэтому три поля обязательны, а не
желательны.

### Поправка 2 (2026-08-05) — ключи статики наружу не выставляются

Сопоставление живёт ВНУТРИ `SceneCapture.Capture` — решение Р1(a). Схема
ключей `#NN`/`#rb` НЕ выставляется ни на `SceneCaptureResult`, ни на любой
другой публичной поверхности: ключи остаются там, где они сегодня —
в приватном `StaticCandidate.Key` (`SceneCapture.cs:249-253`, места
присвоения `SceneCapture.cs:522` и `SceneCapture.cs:599`), а
`SceneCaptureResult.StaticColliders` по-прежнему отдаёт только определения
(`SceneCapture.cs:149`).

**Прежняя плановая формулировка о выставлении ключей статики наружу
ОТОЗВАНА как несовместимая с Р1(a)** — записано явно, чтобы вопрос не
пере-открывался. Ратифицированного текста контракта отзыв не касается:
раздел «Сопоставление и инъекция» ссылался на существующую схему
(`SceneCapture.cs:597-602`) и выставления наружу не требовал.

### Поправка 3 (2026-08-05) — структурная точка входа запуска

*(Зачин поправки реконструирован — см. «Провенанс».)* Отказ ДО входа в
Play Mode не пишет капсулу улик и не создаёт каталог рана; поэтому код
отказа (`SNAPSHOT_CAPTURE_FAILED`, решение №5) возвращается **структурно,
рядом с причиной отказа**, а не через капсулу.

Форма продиктована ЗАМЕРОМ, не предпочтением: второй публичный статический
перегруз с именем `TryStartTowerSearch` вызывает `AmbiguousMatchException`
на четырёх существующих рефлексионных вызовах `GetMethod(name,
Public | Static)` — `GhostEvidenceTests.cs:547-551`,
`GhostEvidenceTests.cs:642-645`, `GhostEvidenceTests.cs:692-695`,
`SearchEntryParameterizationTests.cs:377-378` (все четыре сверены в этом
репозитории при внесении поправки). Отсюда:

- новая структурная точка входа **непубличная И с другим именем**;
- существующая сигнатура `public static bool TryStartTowerSearch(
  GhostSearchEntry entry, string source, out string rejectReason)`
  (`GhostEvidencePlayModeHost.cs:78-81`) остаётся **байт-в-байт
  неизменной**;
- существующий метод **делегирует** в новую структурную точку входа.

**Явно исключено:** второй метод с тем же именем; необязательные
параметры; любое изменение арности или видимости существующего метода;
замена `out string rejectReason` на результат-тип; переписывание или
ослабление существующих рефлексионных тестов.

Инвариант П.6 контракта 2.2.2 («`TryStartTowerSearch` — единственный
публичный вход поиска») поправкой сохраняется дословно: новая точка входа
непубличная.

### Поправка 4 (2026-08-05) — сравниваемые множества

*(Зачин поправки реконструирован — см. «Провенанс».)* Снимок использует
**тот же критерий членства, что и обычный захват, минус меш-резолв
геометрии**. Объекты со статусом `ExcludedSafely` (`SceneCapture.cs:24`;
бесконтактные тела и т.п.) в сравниваемые множества НЕ входят.

Тела и статики сравниваются как **РАЗДЕЛЬНЫЕ множества**:

- тело, присутствующее только на одной стороне, даёт строку 6 или строку 7;
- статика, присутствующая только на одной стороне, даёт строку 10a или
  строку 10b.

**Записано явно: трактовать их как одно множество — неверно.** Общий
счётчик или общее множество ключей смешали бы тело со статикой и выдали бы
литерал не той строки.

### Поправка 5 (2026-08-05) — строка 12 и канал масштаба

Для статик формы **Box и Sphere** исходный `lossyScale` захватывается и
хранится **исключительно как `StaticCandidate.SourceLossyScale` внутри
`SceneCapture.Capture`** и сравнивается **внутри этого же захватного пути —
до конструирования `SceneCaptureResult` и до сериализации handover**.

- Ни поле масштаба, ни параллельный массив, ни эквивалентное свойство, ни
  перегруз **не добавляются к `SceneCaptureResult`** для этой цели.
- **`SimulationStaticColliderDefinition.FullScale` для нешейповых (не-Mesh)
  форм НЕ заполняется.** Поле сохраняет существующую мешевую семантику
  («2.2.2: the real lossyScale for mesh statics»,
  `SimulationHarness.cs:234-235`; присвоение — только в мешевой ветке,
  `SceneCapture.cs:835`). Изменение этого поля изменило бы смысл
  существующего поля, его DTO-представление и поведение будущих
  потребителей.

**Замер, разрешающий такую форму** (`Library/BugCamEvidence/probe-2.2.3-v1-pins/REPORT.md`,
2026-08-05, живой редактор Unity `6000.3.21f1`):

- пины 1, 2 и 3 — бит-идентичны;
- field-diff по metrics и manifest — ноль как для башни, так и для домино
  (кроме документированных `runId`/`builtUtc`/`runDirectory`/`scenePath`);
- EditMode `157` и PlayMode `27` — без изменений, ровно базовые;
- пин round-trip handover (`SceneCaptureHandoverRoundTripsBitIdentically`)
  зелёный, `ObjectScaleChannelBitIdentityPin` зелёный;
- инъецированный дрейф в 1 ULP (`2.0f` против `2.0000002f`) на
  `GateScene222` пометил все четыре статики по РЕАЛЬНЫМ ключам
  (`0000:Floor#00`, `0001:WallBack#00`, `0002:WallArchedFront#00`,
  `0003:Stairs#00`), чистый проход не пометил ни одной; hash захвата в обоих
  проходах одинаков — канал в hash не входит.

**Оговорка, поднятая самим замеряющим агентом и записываемая здесь:** пин 1
идёт факторным путём (`TowerProbeRequestFactory.CreateBaseline`) и в
`SceneCapture.Capture` не заходит вовсе — по построению он к этой правке
иммунен; **дискриминирующие пины — 2 и 3**.

**Записывается также:** код пробы был реконструирован из ограничений и
никогда не коммитился (ветка `probe/2.2.3-pins` удалена, рабочее дерево
возвращено к `main` `7cf34f6`). Именно поэтому место сравнения и место
канала закреплены текстом контракта, а не оставлены на усмотрение
исполнителя.

### Уточнение к строкам 9 и 12 (передано тем же сообщением 2026-08-05)

Строки 9 и 12 таблицы отказов печатают **полный `Vector3`, три компоненты,
формат `"R"`, `InvariantCulture`**. `objectScale` использовать НЕЛЬЗЯ: он
сворачивает вектор в одну компоненту и пропустил бы анизотропное изменение
масштаба.

## Выходной гейт блока (Адъюдикация 2026-08-04, решение №7 — ратифицировано)

1. **Сцена: `Assets/BugCam/Tests/GateScene222.unity`** (заморожена решением
   2.2.2 — используется строго read-only, ни одного изменения файла сцены).
   Гейт-драйвер шагает живую сессию ~100 фиксированных шагов (2.0 с —
   середина обрушения), снимок, выход, поиск с дефолтами end-to-end.
   **Прецедент A8 действует целиком (решение №7): какой вердикт вышел — тот
   и результат; тюнинг сцены ИЛИ момента снимка ради более красивого
   вердикта запрещён.** Драйвер — временный, некоммитимый, test-only
   (решение №11) и обязан восстановить `Physics.simulationMode = Script`
   до выхода.
2. **Артефакт:** каталог рана `Library/BugCamEvidence/Runs/<run-id>/` с
   манифестом, несущим секцию снимка и meshRef; в отчёте — подтверждение, что
   в снимке ≥1 тела с ненулевой линейной И ≥1 с ненулевой угловой скоростью
   (каналы доказаны данными, не тестом); честный вердикт с
   вердикт-STOP-семантикой A8/2.2.2 (STABLE / DIVERGENT AT SEARCH FLOOR =
   остановка и решение человека); **evidence гейта явно фиксирует, что
   сессию шагал временный драйвер (решение №11) — результат не читается как
   доказательство для дефолтного FixedUpdate-проекта**.
3. **Контроли бит-в-бит:** башня (пин 1), домино (пин 2), edit-mode-захват
   GateScene222 (пин 3).
4. Batchmode целиком зелёный с выросшими счётчиками; счётчики и evidence-пути
   фиксируются в STATUS.

## Backlog-записи блока (записаны адъюдикацией 2026-08-04; кода не подразумевают)

1. **Решение №2:** захват примитивных (Box/Sphere) runtime-спаунов по шейпу
   в снимке — кандидат на поздний блок; в v0.1 runtime-объекты fail-closed.
2. **Решение №11 (обязательный; backlog до завершения остального блока):**
   проверить снапшот-путь в проекте с дефолтным `simulationMode =
   FixedUpdate` ДО любого заявления об общей поддержке; гейт этого блока
   идёт под временным драйвером и такого заявления не даёт.

## Открытые вопросы — ВСЕ ЗАКРЫТЫ (адъюдикация 2026-08-04, включая №9 и развилку строки 11)

> Решения человека 2026-08-04, финальны для блока, если явно не пере-открыты.
> Формулировки внесены в разделы выше; здесь — реестр.

1. **Поток и П.6:** ЗАКРЫТ — принято как рекомендовано: снимок → выход из
   Play Mode → стандартный edit-mode-захват при запуске; П.6 дословно, без
   поправок. Отвергнутая альтернатива и её причина (hash невычислим в Play
   Mode, эмпирика 2026-08-04) записаны, чтобы вопрос не пере-открывался.
2. **Runtime-spawned тела:** ЗАКРЫТ — fail-closed; два условия: сообщение
   поимённо называет тела-нарушители и заявляет границу охвата v0.1 (не
   дефект); backlog-запись о примитивных спаунах по шейпу (без кода).
3. **Позы статики/кинематики:** ЗАКРЫТ — позы из снимка (принуждение к
   edit-mode-позам дало бы внутренне противоречивое начальное состояние);
   поправка: расхождение position/rotation фиксируется в метриках рана,
   расхождение МАСШТАБА = fail-closed (строка 12); допуск сравнения масштаба
   заявлен явно — нулевой, бит-в-бит.
4. **Сон:** ЗАКРЫТ условно на П-2, как написано — перенос через
   `Rigidbody.Sleep()` при зелёной П-2, иначе прецедент A2; взятая ветка
   обязана быть названа в вердикте и капсуле, молчаливый откат запрещён.
5. **Код ошибки:** ЗАКРЫТ — новый `SNAPSHOT_CAPTURE_FAILED` (прецедент
   решения №5 контракта 2.2.2).
6. **Хэш:** ЗАКРЫТ — инъекция ДО `ComputeHash`; хвост `|state:` только на
   снапшот-пути; несущее свойство (edit-mode-строки без изменений, пины
   башни и домино байт-идентичны) закрепляется прямым регрессионным тестом,
   а не косвенно через существующие пин-тесты.
7. **Гейт-сцена:** ЗАКРЫТ — GateScene222 read-only, снимок в середине
   обрушения; прецедент A8 целиком: какой вердикт вышел — тот и результат,
   тюнинг сцены или момента снимка ради вердикта запрещён.
8. **Представление снапшот-рана:** ЗАКРЫТ —
   `GhostSearchSceneKind.SnapshotScene = 2`, аддитивно.
9. **Литералы:** ЗАКРЫТ — РАТИФИЦИРОВАН 2026-08-04 с четырьмя поправками
   (литерал 6 — хвост «граница охвата, не дефект»; литерал 10 — разделён на
   10a/10b, встроенное условие в user-facing строке отвергнуто; литерал 11 —
   заменён по развилке; строка капсулы — дополнена веткой сна); литералы
   1–5, 7–9, 12 — дословно как переданы отчётом. Языковой прецедент сверен:
   русский, как в 2.2.1/2.2.2.
9-bis. **Развилка строки 11 (по данным П-1):** ЗАКРЫТА — против fail-closed
   на дефолтных 50 rad/s; подъём предела как верность состоянию: снимок
   несёт per-body `maxAngularVelocity`, `CreateBody` применяет его;
   fail-closed только при превышении собственного предела тела (истинно
   невоспроизводимое состояние — кламп первого шага, расхождение с живой
   сессией с шага 0).
10. **Пауза при снимке:** ЗАКРЫТ — пауза не обязательна; требование: снимок
    записывает индекс физического шага и `Time.fixedTime`, чтение — один
    синхронный проход без yield'ов; риск покрыт П-3.
11. **Гейт-драйвер среды (новый, по находке `m_SimulationMode: 2`):**
    ЗАКРЫТ — драйвер test-only и никогда не попадает в плагин; evidence
    гейта фиксирует временный драйвер (результат — не доказательство для
    дефолтного проекта); обязательный backlog-пункт: проверка в
    FixedUpdate-проекте до заявлений об общей поддержке.

## Непроверенные утверждения — статус после замеров 2026-08-04

Закрыто замерами (числа — в статус-блоке предпосылок): кламп
`maxAngularVelocity` (П-1: на первом `Simulate`, не при присвоении);
устойчивость `Rigidbody.Sleep()` (П-2: держится, обнуляет скорости);
семантика Rigidbody-API при интерполяции (П-4: transform расходится до
0.274 м, rb-API бит-стабилен в колбэке); несериализуемость рантайм-присвоения
`Physics.simulationMode` (П-5: не дирти́т ассет, авто-откат при выходе).

Остаётся непроверенным:

- Способ получения индекса физического шага: прямой публичный счётчик
  завершённых fixed-шагов не верифицирован; кандидат-вычисление
  `Time.fixedTime / Time.fixedDeltaTime` ЗАМЕРЕНО даёт почти-целые
  (ошибка ~2×10⁻⁶ на ~3-минутной сессии) — требуется округление, дрейф на
  долгих сессиях не замерялся; реализация вправе предпочесть собственный
  счётчик (решение №10 требует само поле, не конкретный API).
- Атомарность синхронного main-thread-прохода относительно шага физики
  заявлена из однопоточности player loop; бит-стабильность двойного чтения
  в одном колбэке (П-4, 4/4) её подкрепляет, но формального пина в
  репозитории нет — добавляется тестом при реализации.
- **Открытый вопрос реализации, поднятый агентом 2026-08-05 (поправке 5 НЕ
  принадлежит — поправка о нём молчит):** способ, которым снимочная таблица
  масштабов статик попадает ВНУТРЬ `SceneCapture.Capture`, поправкой 5 не
  зафиксирован — проба подавала её некоммиченным полем
  (`ProbeSnapshotScales`, удалено вместе с пробой). Поправка 5 фиксирует
  место сравнения и место канала, но о входном канале не говорит ничего;
  выбор формы входа — на реализацию, при неизменных `SceneCaptureResult`,
  `ComputeHash` и публичных сигнатурах (ограничение поправки 5 действует).
